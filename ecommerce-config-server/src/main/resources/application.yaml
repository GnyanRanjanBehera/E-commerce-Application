server:
  port: 9001
spring:
  application:
    name: ecommerce-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GnyanRanjanBehera/Task-Pay-Config.git
          clone-on-start: true
